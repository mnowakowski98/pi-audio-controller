# Remote Audio Controller - App

## Hosting
Literally just host the directory somewhere idk.

## Usage
TODO


# Remote Audio Controller - Server

## Running
> Node.js must be installed
- Extract the archive
- Run `npm install --omit=dev`
- Run `npm start`